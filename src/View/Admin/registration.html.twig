{% extends 'layoutAdmin.html.twig' %}

{% block stylesheet %}{% endblock %}

{% block content %}
	<div class="container mt-5">
		<div class="row d-flex justify-content-center">
			<div class="col-10">
				<h1 class="adminMembersTitle text-danger text-center fs-1 mt-5">Selection participant</h1>

				<table class="table table-striped table-hover">
					<thead>
						<tr class="">
							<th scope="col">Prénom</th>
							<th scope="col">Nom</th>
							<th scope="col"></th>
						</tr>
					</thead>
					<tbody>
						{% for registration in registrations %}
							<tr>
								<td class="col-md-2">{{ registration.firstname }}</td>
								<td class="col-md-2">{{ registration.lastname }}</td>
								<td class="col-md-2">
									<form action="" method="POST">
										<input type="checkbox" class="form-check-input" name="Inscrit" value="{{registration.id}}" checked>
									</form>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endblock %}
